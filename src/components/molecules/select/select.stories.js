import React from "react";

import CustomizedHook from "./select";

export default {
  title: "molecules/SelectJobLocation",
  component: CustomizedHook,
};
const Template = (args) => <CustomizedHook {...args} />;
export const Primary = Template.bind({});
