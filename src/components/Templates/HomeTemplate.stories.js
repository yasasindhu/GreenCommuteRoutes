import React from "react";

import HomeTemplate from "./HomeTemplate";




export default {
  title: "templates/HomeTemplate",
  component: HomeTemplate,
};
const Template = (args) => <HomeTemplate />;

export const PrimaryTemplate = Template.bind({});
